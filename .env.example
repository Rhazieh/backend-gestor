# Backend Gestor - Ejemplo de variables de entorno
# Copiá este archivo a .env y ajustá valores según tu entorno.

# Puerto del servidor HTTP (opcional). Si no se define, usa 3000.
# PORT=3000

# URL de conexión a la base de datos (PostgreSQL)
# Para local con docker-compose (usuario: postgres / pass: postgres / db: turnos):
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/turnos

# Forzar SSL para proveedores como Render/Heroku
# true -> activa SSL (con rejectUnauthorized=false), false/omitir -> sin SSL
# DATABASE_SSL=true
